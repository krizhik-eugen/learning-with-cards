import React from 'react';
import {useDispatch} from 'react-redux';

export const NewPasswordCreation = () => {
    const dispatch = useDispatch()

    return (
        <div>
            New password creation
        </div>
    )
}