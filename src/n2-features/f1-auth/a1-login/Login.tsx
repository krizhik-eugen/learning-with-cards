import React from 'react';
import {useDispatch} from 'react-redux';

export const Login = () => {
    const dispatch = useDispatch()

    return (
        <div>
            Login
        </div>
    )
}