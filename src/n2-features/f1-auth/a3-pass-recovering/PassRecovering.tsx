import React from 'react';
import {useDispatch} from 'react-redux';

export const PassRecovering = () => {
    const dispatch = useDispatch()

    return (
        <div>
            Password recovering
        </div>
    )
}